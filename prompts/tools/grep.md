Search file contents (regex). Pass {"query":"distinctive phrase or regex"}. Optional path (file or folder), glob (e.g. "*.js"), case_insensitive, context (nearby lines, default 2, max 10), head_limit, and output_mode: content (default, matching lines), files (paths only), or count. Invalid regex is treated as literal text. Call several greps in one turn. Use files to discover, content after you have a specific query or path. If content already shows unique lines, edit; do not page the file or list directories to find it.
